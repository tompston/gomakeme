project_info:
  go_version: 1.16
  name: "change_my_name"  # no spaces or / in the name
  port: 5000
  # include_db_snippet: true
  # sqlc: true
modules: [User, Country]  # Title case + singular!
